name: "Bypass_Builder_V4"

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: "Derleme İşlemi (V4)"
    runs-on: macos-latest

    steps:
      - name: "📦 Depoyu Çek"
        uses: actions/checkout@v4

      - name: "🛠 Theos ve Bağımlılıkları Kur"
        run: |
          # ldid ve dpkg kurulumu (İmzalama ve Paketleme için şart)
          brew install ldid dpkg
          
          export THEOS=~/theos
          rm -rf $THEOS
          git clone --recursive https://github.com/theos/theos.git $THEOS
          
          # SDK kurulumu
          rm -rf $THEOS/sdks
          git clone https://github.com/theos/sdks.git $THEOS/sdks

      - name: "⚙️ Çevresel Değişkenleri Ayarla"
        run: |
          echo "THEOS=~/theos" >> $GITHUB_ENV
          echo "~/theos/bin" >> $GITHUB_PATH

      - name: "🏗 Tweak'i Derle"
        run: |
          # -j$(sysctl -n hw.ncpu) komutu derlemeyi tüm çekirdekleri kullanarak hızlandırır
          make package FINALPACKAGE=1 -j$(sysctl -n hw.ncpu)
        env:
          THEOS: ~/theos

      - name: "🚀 .deb Dosyasını Çıktı Ver"
        uses: actions/upload-artifact@v4
        with:
          name: "AnoBypass_V4_Final"
          path: packages/*.deb
