name: "Bypass_Builder_V4"

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: "Derleme İşlemi (V4)"
    runs-on: macos-latest

    steps:
      - name: "📦 Depoyu Çek"
        uses: actions/checkout@v4

      - name: "🛠 Theos ve SDK Kurulumu"
        run: |
          export THEOS=~/theos
          # Eğer klasör varsa temizle (Hata 128 Çözümü)
          rm -rf $THEOS
          
          git clone --recursive https://github.com/theos/theos.git $THEOS
          
          # SDK klasörünü garantiye al ve çek
          rm -rf $THEOS/sdks
          git clone https://github.com/theos/sdks.git $THEOS/sdks

      - name: "⚙️ Çevresel Değişkenleri Ayarla"
        run: |
          echo "THEOS=~/theos" >> $GITHUB_ENV
          echo "~/theos/bin" >> $GITHUB_PATH

      - name: "🏗 Tweak'i Derle"
        run: |
          # Theos'un yolu ve derleme emri
          make package FINALPACKAGE=1
        env:
          THEOS: ~/theos

      - name: "🚀 .deb Dosyasını Çıktı Ver"
        uses: actions/upload-artifact@v4
        with:
          name: "AnoBypass_V4_Final"
          path: packages/*.deb
